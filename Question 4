/*Write a function named addarrays that accepts two arrays that are of the same size .The function should add each element in the arrays together and place them in the third array*/#include <stdio.h>int addarrays(int array1[]  ,int array[] ,int array3[],int capacity){    int p;for( p=0;p<capacity;p++){    array3[p]=array1[p]+array2[p];}}
/* Modify the function you created in to return a pointer to the array containing the totals. place this place this in a program that displays and also displays the values in all the three arrays*/#include<stdio.h>int *addarrays(int array1[],int array2[],int array3[],int capacity);main(){    int p;    int array1[3]={4,5,6};    int array2[3]={7,8,9};    int *array3[]=addarrays(array1[],array2[],3);    for(p=0;p<3;p++)    {    printf("%d\n",array3[p]);    }}int *addarrays(int array1[],int array2[],int array3[],int width){    int p;int *array3[]=malloc(width*capacityof(int));
for(p=0;p<width;p++){    array3[p]=array1[p]+array[2];}return array3;}
