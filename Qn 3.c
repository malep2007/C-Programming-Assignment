#include <stdio.h>
#include <stdlib.h>
int main()

/*Qn3 a&b
{float x[50];
   x[50]=123.456;
   printf("%d",x[50]);
  return 0;
  }
  
/*Qn 3c
{
int x;
for (x=0; x<100; x++)
printf("%d\t",x);
return 0;} the value of x is 99*/

/*Qn 3d 
{
int ctr;
for(ctr=2; ctr<10; ctr+=3)
printf("%d\t",ctr);
return 0;}
 the values of ctr are 2,5 and 8*/

/*Qn 3e
{int x=1
while(x=1;count<=100;x++;)*/

 /*Qn 3 f
The semi-colon on the for loop makes the program  output only one value which is initialized to the counter variable in the for loop.
The output will only show "Counter = 1" instead of iterative values.
 */   
