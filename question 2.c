a) float do_it(char argument1, char argument2, char argument3);

b) void print_a_number(int number);

c) the main is not supposed to be returning anything but it returns an integer. the function print_message has a void return type yet the funtion definition is returning an integer
