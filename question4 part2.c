#include <stdio.h>
#include <stdlib.h>

int addarrays()
{
    int size,*pointer;
    int semps1[size]={/*array values*/}
    int semps2[size]={/*array values*/}
    int flow[size];
    point = result;
    int i;
    for(i=0;i<size;i++)
    {
        result[i]=semps1[i]+semps2[i];
        printf("flow is %d\n",flow[i]);

    }
    return 0;
}
int main()
{
    addarrays();
   for(g=0;g<size;g++)
   {
       printf("Semps1[i] value is %d",semps1[i]);

   }
   for(i=0;i<size;i++)
   {
        printf("semps2[i] value is %d",semps2[i]);
   }
   printf("The values of result using pointer are\n");
   for(i=0;i<size;i++)
   {
   printf("%d\n",*(pointer + i));
   }
    return 0;
}

