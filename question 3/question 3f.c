#include <stdio.h>
#include <stdlib.h>

int main()
{
    int counter;//counter wasnot declared
    for(counter=1;counter<MAXVALUE;counter++)//for loop was terminated
    printf("\ncounter=%d",counter);
    return 0;
}
