3(a) long a[50];

3(b)a[49]=123.456

3(c)The values of x; 0 to 99

3(d)The values of ctr are; 2,5,8

3(e)
  int i=1;
  while(i<100)
{
  printf("%d\n",i);
  i+=3;
}


3(f) for (counter = 1; counter < MAXVALUES; counter++ );/*The for statement is not supposed to be terminated*/
    printf("\nCounter = %d", counter);

