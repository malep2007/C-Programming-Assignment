long musa[50]; //declared array


musa[49]=123.456;// assigning value to 50th element


 for (x = 0; x < 100; x++) //the last value of x is 100


  for(ctr = 2; ctr < 10; ctr += 3) // the value of ctr will be 11 when the statement is complete.


  int d= 1;
  int counter;
  while(d <=100){
  counter=d;
  d+=3;

  }

  /*What is wrong with the program*/

  for (counter = 1; counter < MAXVALUES; counter++ );//the code has no error.
  printf("\nCounter = %d", counter);
