 // PART A
  int marks[50];

  // PART B
  marks[49]=123.456

  // PART C
   X will have values from 0 to 99 with an increment of 1

   // PART D
   ctr = {2, 5, 8}
   
   // PART E
   #include <stdio.h>
main(){
int count = 1;
while(count <= 100){
printf("%d ", count);
count += 3;
}
}

   // PART F
   Termination of the loop with a semicolon
