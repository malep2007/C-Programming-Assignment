/* A header of a function named do_it()that takes three type char arguments and returns a type float to the calling function*/(do_it.h)#ifnedf DO_IT_H#define DO_IT_Hfloat do_it(char p,char q,char r)#endif// DO_IT_H
/* A header of a function named print_a_number()that takes a single type int argument and does not return anything to the calling function*/(print_a_number.h)#ifndef PRINT_A_NUMBER_H#define PRINT_A_NUMBER_Hvoid print_a_number(int)#endif// PRINT_A_NUMBER_H
/* The following description in comments are what is wrong with the program*/#include<stdio.h>void print_msg(void);main(){    print_msg("This is a message to print");/* The print_msg function is not supposed to take in any argument but the main function call it with a string argument argument*/    return 0;}void print_msg(void){    puts("This is a message to print");    return 0;}
