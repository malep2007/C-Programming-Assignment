#include <stdio.h>
#include <stdlib.h>

    int x=1;/*wrong termination was used in question, : instead of ;*/



int main()
{

   if (x==1)/*there were two errors on this line, first the termination of the if function and then the use of an assignment
               operator instead of a relational operator ==*/


    printf(" x equals 1");

    else/*the error on this line was the use of otherwise which is not a condition*/

    printf("x doesn't equal to 1");

    return 0;

}
